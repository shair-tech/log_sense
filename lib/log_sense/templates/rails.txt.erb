* Rails Log Analysis

<%= render "summary.txt.erb", data: data %>

<% @reports.each do |report| %>
** <%= report[:title] %>

<%=
table = Terminal::Table.new headings: report[:header], rows: report[:rows]
(0..report[:header].size - 1).map { |i| table.align_column(i, report[:column_alignment][i]) }
table
%>
<% end %>
** Command Invocation

<%= render "command_invocation.txt.erb", data: data %>

** Log Sense Performance

<%= render "performance.txt.erb", data: data %>

